#include <stdio.h>
#include <stdlib.h>

int main() {    
  double raio, area, pi;
  pi = 3.14159;
  printf("");
  scanf("%lf", &raio);
  area = pi * (raio * raio);
  printf("A=%.4lf\n", area);

  return 0;

    
}
