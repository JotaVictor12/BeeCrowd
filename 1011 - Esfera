#include <stdio.h>
 
int main() {
    
   double raio, volume, r3, pi;
   pi = 3.14159;
   scanf("%lf", &raio);
   r3 = raio * raio * raio;
   volume = (4/3.0) * pi * r3;
   
   printf("VOLUME = %.3lf\n", volume);
   
 
    return 0;
}
